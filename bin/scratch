#!/usr/bin/env bash

alias rails >/dev/null 2>&1 || alias rails="bundle exec bin/rails"

# make sure we're in the project root
cd `dirname $0`/..

generator_options="--no-helper --no-view-specs --no-helper-specs --no-assets --no-stylesheets"

rails g migration remove_quire_id_from_leaves quire_id:integer

#rails g migration remove_single_certainty_from_leaves single_certainty:integer

#rails g migration make_certainties_default_to_high_in_leaves

#rails g migration remove_position_from_leaves position:integer

#rails g migration rename_folio_certainty_to_folio_number_certainty_in_leaves

#rails g migration add_mode_folio_certainty_to_leaves folio_certainty:integer \
#    mode_certainty:integer \
#    single_certainty:integer

#rails g migration remove_conjoin_id_from_quire_leaves conjoin_id:integer

#rails g migration remove_number_from_quires number:string

#rails g migration remove_mode_from_quire_leaves mode:string

#rails g migration add_sub_quire_to_quire_leaves \
#  sub_quire:integer

# rails g migration add_parent_quire_to_quires quire_id:integer

# rails g migration convert_old_leaves_to_new
# rails g migration add_position_to_quire_leaves \
#   position:integer

# rails g model quire_leaf leaf:references \
#   quire:references \
#   certainty:integer