<p id="notice"><%= notice %></p>

<p><%= render 'bread_crumbs' %></p>

<h1><%= @quire.name %></h1>

<%= render 'manuscripts/summary', manuscript: @quire.manuscript %>

<div class="row">
  <div class="col-sm-2">
    <p class="field-label">Quire</p>
  </div>
  <div class="col-sm-10">
    <p class="field-value"><%= @quire.number %></p>
  </div>
</div>

<div>
  <%= link_to 'Edit', edit_quire_path(@quire), class: 'btn btn-default' %>
</div>


<% if @quire.quire_leaves.present? -%>
  <h3>Leaves</h3>
  <% @quire.quire_leaves.each do |quire_leaf| %>
    <div class="row">
      <div class="col-sm-1">
        Leaf <%= quire_leaf.position %>
      </div>
      <div class="col-sm-1">
        Folio: <%= quire_leaf.folio_number %>
      </div>
      <div class="col-sm-2">
        Mode: <%= quire_leaf.mode %>
      </div>
      <div class="col-sm-2">
        Single? <%= quire_leaf.leaf_single.yesno %>
      </div>
    </div>
  <% end %>
<% end -%>
